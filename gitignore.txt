# OS-specific files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Logs and temp files
*.log
*.tmp
*.swp

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
env/
venv/
ENV/
.Python

# Java
*.class
*.jar
*.war
*.ear
target/
build/

# C/C++
*.o
*.obj
*.exe
*.dll
*.so
*.out

# Dotnet
bin/
obj/

# Ruby
*.gem
.bundle/
log/
tmp/

# Go
bin/
*.exe

# Mac
*.DS_Store

# Backup & Misc
*.bak
*.tmp
*.orig
*.old

# Package distribution
*.tgz
*.tar.gz
*.zip

# Ignore files by size (over 50 MB)
# This requires 'git check-attr' or pre-commit hooks; .gitignore can't block by size directly.
# Recommendation: Use .gitattributes or a pre-commit hook for this.
# Example pre-commit hook (put in .git/hooks/pre-commit, set as executable):
# #!/bin/sh
# files=$(git diff --cached --name-only | xargs -I{} du -m {} | awk '$1 > 50 {print $2}')
# if [ -n "$files" ]; then
#   echo "Error: The following files are over 50MB and will not be committed:"
#   echo "$files"
#   exit 1
# fi

# Git
.git/
.gitignore
.gitattributes

# Others
*.sqlite
*.db
*.pdf